const galleryGrid=document.getElementById('galleryGrid'),lightbox=document.getElementById('mediaLightbox'),lightboxMedia=document.getElementById('lightboxMedia'),lightboxTitle=document.getElementById('lightboxTitle');function youtubeId(url){try{const u=new URL(url,location.origin);if(u.hostname.includes('youtu.be'))return u.pathname.slice(1);if(u.hostname.includes('youtube.com'))return u.searchParams.get('v')||u.pathname.split('/embed/')[1]}catch{}return null}function openMedia(item){lightboxMedia.replaceChildren();const yt=youtubeId(item.url);let media;if(item.type==='video'&&yt){media=document.createElement('iframe');media.src=`https://www.youtube.com/embed/${yt}?autoplay=1`;media.allow='autoplay; fullscreen';media.allowFullscreen=true}else if(item.type==='video'){media=document.createElement('video');media.src=item.url;media.controls=true;media.autoplay=true}else{media=document.createElement('img');media.src=item.url;media.alt=item.alt_text||item.title}lightboxMedia.append(media);lightboxTitle.textContent=item.title;lightbox.hidden=false;document.body.style.overflow='hidden'}function closeMedia(){lightbox.hidden=true;lightboxMedia.replaceChildren();document.body.style.overflow=''}document.getElementById('lightboxClose').addEventListener('click',closeMedia);lightbox.addEventListener('mousedown',e=>{if(e.target===lightbox)closeMedia()});document.addEventListener('keydown',e=>{if(e.key==='Escape'&&!lightbox.hidden)closeMedia()});
function applyFilter(filter){galleryGrid.querySelectorAll('.gallery-item').forEach(item=>item.hidden=filter!=='all'&&item.dataset.category!==filter)}document.querySelectorAll('.gallery-filters button').forEach(button=>button.addEventListener('click',()=>{document.querySelector('.gallery-filters .active').classList.remove('active');button.classList.add('active');applyFilter(button.dataset.filter)}));
function render(items){galleryGrid.replaceChildren();items.forEach(item=>{const article=document.createElement('article');article.className='gallery-item';article.dataset.category=item.type==='video'?'video':item.category;const button=document.createElement('button');let media;const yt=item.type==='video'&&youtubeId(item.url);if(yt){media=document.createElement('img');media.src=`https://img.youtube.com/vi/${yt}/hqdefault.jpg`;media.alt=item.alt_text||item.title}else if(item.type==='video'){media=document.createElement('video');media.src=item.url;media.muted=true;media.preload='metadata'}else{media=document.createElement('img');media.src=item.url;media.alt=item.alt_text||item.title;media.loading='lazy'}const caption=document.createElement('span'),title=document.createElement('b'),category=document.createElement('small');title.textContent=item.title;category.textContent=item.type==='video'?'Video':item.category;caption.append(title,category);button.append(media,caption);button.addEventListener('click',()=>openMedia(item));article.append(button);galleryGrid.append(article)})}fetch('/api/gallery').then(response=>response.ok?response.json():Promise.reject()).then(render).catch(()=>galleryGrid.innerHTML='<p>The gallery is taking a short pause. Please try again soon.</p>');
