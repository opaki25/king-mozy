const menuButton=document.getElementById('menuButton'),navLinks=document.getElementById('navLinks');if(menuButton&&navLinks){menuButton.addEventListener('click',()=>{const open=navLinks.classList.toggle('open');menuButton.textContent=open?'Close':'Menu';menuButton.setAttribute('aria-expanded',String(open))});navLinks.querySelectorAll('a').forEach(link=>link.addEventListener('click',()=>{navLinks.classList.remove('open');menuButton.textContent='Menu';menuButton.setAttribute('aria-expanded','false')}))}document.querySelectorAll('.current-year').forEach(element=>element.textContent=new Date().getFullYear());
const contactForm=document.getElementById('booking-form');if(contactForm){const requestedTrip=new URLSearchParams(location.search).get('trip');if(requestedTrip&&document.getElementById('contactTrip'))document.getElementById('contactTrip').value=requestedTrip;contactForm.addEventListener('submit',event=>{event.preventDefault();const data=new FormData(contactForm),subject=encodeURIComponent(`Tour enquiry: ${data.get('trip')}`),body=encodeURIComponent(`Hello King Mozy Tours and Travel,\n\nI would like to plan a trip.\n\nName: ${data.get('name')}\nEmail / phone: ${data.get('contact')}\nTrip: ${data.get('trip')}\nTravel month: ${data.get('month')}\nGuests: ${data.get('guests')}\n\nNotes: ${data.get('notes')||'No additional notes'}`);location.href=`mailto:kingmozytoursandtravel@gmail.com?subject=${subject}&body=${body}`})}
